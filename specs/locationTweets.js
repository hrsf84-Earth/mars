// locationTweets.js
// These are the plain English language tests I would like to perform.

// The widget should be a stateful component and get the query term & lat/long states from the store.
// The widget should not appear at all if no movie is selected
// The widget should accept the lat/long format as input and render data.
// The widget should accept a query search term as input and render data.
// The widget should render a simple bar graph of data.
// The widget should be in the specified color scheme

// The widget should paint sentiment information with a fixed tweet file
//  (need to figure out what the outcome data should be if we send it to watson)